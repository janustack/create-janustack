# https://moonrepo.dev/docs/config/workspace
$schema: 'https://moonrepo.dev/schemas/workspace.json'

generator:
  templates:
    - 'git://github.com/create-janustack/templates#main'

projects:
  - 'packages/*'

vcs:
  manager: 'git'
  defaultBranch: 'main'
